# Markdown live-preview parity fixtures

Fixtures in this directory are source-of-truth Markdown documents for the Atomic Editor port parity matrix.
They are intentionally raw Markdown so copy/save/round-trip behavior can be tested against source text.
